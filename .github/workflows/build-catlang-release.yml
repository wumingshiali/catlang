name: Build CatLang (Release)

permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (optional)'
        required: false
        default: 'latest'

jobs:
  build:
    name: Release Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build (Release with max optimizations)
        run: make build

      - name: Run tests
        run: make test

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: catc-release-${{ runner.os }}-${{ runner.arch }}
          path: |
            target/release/catc
            target/release/catc.exe
          if-no-files-found: ignore
          retention-days: 30
